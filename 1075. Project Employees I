# Write your MySQL query statement below
select project.project_id,
round(avg(employee.experience_years),2) 
average_years from project left join employee 
on project.employee_id=employee.employee_id 
group by project_id
